
fn stringToCharCodes(str)
{
	return str.split("").map(fn(v) { 
		return v.get_code_at(0);
	});

}

fn printTo(con)
{
	// convert "Seppo" to integers
	con.log(stringToCharCodes("Seppo"));
	return con;
}

fn main()
{
	printTo(console).log("hello after printTo");
	return true;
}