
fn createActions()
{
    let actions = [];
    for(let i = 0; i < 3; i++){
        actions.push(() => console.log(`value: ${i}`));
    }

    return actions;
}

fn main()
{

    for(const action : createActions()){
        action();
    }

    return 1;
}