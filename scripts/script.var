fn main() 
{
	let array1 = [5, 12, 50, 130, 44];

	let found = array1.find_last(fn(element) { 
		return element > 45; 
	});

	let arr = [1,2,3];
	let brev = arr.push(4);

	console.log(arr, brev, found);
}
