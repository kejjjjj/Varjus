fn isPrime(n) {
    if (n < 2) 
        return false;

    for (let i = 2; i <= math.sqrt(n); i++) {
        if (n % i === 0) 
            return false;
    }

    return true;
}
fn main()
{
    return [ isPrime(29), isPrime(12) ];   
}