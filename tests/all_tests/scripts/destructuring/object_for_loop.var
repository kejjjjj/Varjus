fn main()
{
    const results = [];
    for(const {x, y} = { x: {a: 5, b: 6}, y: {c: 7, d: 8} };results.length < 4u;){
        const {a, b} = x;
        const {c, d} = y;
        results.push(a);
        results.push(b);
        results.push(c);
        results.push(d);
    }

    return results;
}