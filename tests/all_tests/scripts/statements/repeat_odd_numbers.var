//using array.push might be too much for this simple test

fn main()
{	
	let results = [0,0,0];
	let currentValue = 0;
	let i = 0;
	
	repeat {
		i++;
		if(i % 2 == 0)
			continue;

		results[currentValue++] = i;
		
		if(i >= 5)
			break;

	}

	return results;
}